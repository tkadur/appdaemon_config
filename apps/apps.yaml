global_modules:
  - base_app
  - hue_event
  - lights
  - curve
  - light_setting
  - metrics
  - switch
  - util

emit_metrics:
  module: emit_metrics
  class: EmitMetrics
  global_dependencies:
    - base_app
    - curve
    - light_setting
    - metrics

process_switch_events:
  module: process_switch_events
  class: ProcessSwitchEvents
  global_dependencies:
    - base_app
    - hue_event
    - metrics
    - switch
    - util

time_based_color:
  module: time_based_color
  class: TimeBasedColor
  global_dependencies:
    - base_app
    - hue_event
    - lights
    - curve
    - light_setting
    - switch
    - util

reset_switches_every_night:
  module: reset_switches_every_night
  class: ResetSwitchesEveryNight
  global_dependencies:
    - base_app
    - curve
    - lights
    - switch
